name: "Setup uv"
inputs:
  python-version:
    required: false
    default: ""
runs:
  using: "composite"
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v8.0.0
      with:
        version: "0.11.6"
        python-version: ${{ inputs.python-version }}
