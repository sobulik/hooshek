name: "Setup uv"
inputs:
  python-version:
    required: true
runs:
  using: "composite"
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "0.11.6"
        python-version: ${{ inputs.python-version }}
        enable-cache: true
